# Chrome 商店隐私权填写内容

## 1. 单一用途描述 (Single Purpose)

> 在 Chrome 开发者控制台 → 隐私权惯例 → 单一用途

```
AutoFormX 是一款 AI 驱动的表单自动填写工具。它的单一用途是：使用人工智能技术自动识别网页表单字段类型，并生成符合业务场景的测试数据进行填充，帮助开发者和测试人员提高测试效率。
```

---

## 2. 权限使用说明 (Permission Justification)

> 在 Chrome 开发者控制台 → 隐私权惯例 → 权限

### Host Permission (<all_urls>) 说明：

```
本扩展需要 <all_urls> 权限，因为用户可能需要在任意网站的表单上使用自动填写功能。作为一个表单填写工具，扩展必须能够访问用户当前浏览的任何包含表单的网页，以便识别表单字段并填充测试数据。扩展不会在后台访问任何网站，仅在用户主动触发填写功能时才会与当前页面交互。
```

### Storage 权限说明：

```
用于在用户本地浏览器中安全存储 API 配置信息（如 API Key、模型选择）和用户偏好设置。所有数据仅存储在本地，不会上传到任何外部服务器。
```

### ActiveTab 权限说明：

```
用于获取用户当前活跃标签页的信息，以便识别页面上的表单字段并执行填写操作。仅在用户主动点击扩展或使用快捷键时才会访问当前标签页。
```

### Scripting 权限说明：

```
用于在用户当前浏览的网页中注入内容脚本，实现表单字段的检测和自动填写功能。脚本仅在用户触发填写操作时运行。
```

---

## 3. 隐私权政策 URL

将 PRIVACY_POLICY.md 上传到 GitHub 后，填写：

```
https://github.com/[你的用户名]/AutoFormX/blob/main/PRIVACY_POLICY.md
```

或者如果你有独立网站：

```
https://[你的域名]/privacy-policy
```

---

## 4. 数据使用披露 (Data Use Disclosure)

> 在 Chrome 开发者控制台 → 隐私权惯例 → 数据使用

### 4.1 本扩展是否收集或使用用户数据？

选择：**是**（因为会发送数据到第三方 AI 服务）

### 4.2 数据使用认证

需要勾选以下认证声明：

```
☑️ 我方披露我的数据使用符合 Chrome 网上应用店开发者协议
☑️ 我方披露我的数据使用符合数据使用要求
```

### 4.3 收集的数据类型

勾选适用项：

| 数据类型 | 是否收集 | 说明 |
|---------|---------|------|
| 个人身份信息 | ❌ 否 | |
| 健康信息 | ❌ 否 | |
| 财务和支付信息 | ❌ 否 | |
| 身份验证信息 | ❌ 否 | |
| 个人通信 | ❌ 否 | |
| 位置信息 | ❌ 否 | |
| 网站浏览活动 | ❌ 否 | |
| 用户活动 | ❌ 否 | |
| 网站内容 | ✅ 是 | 表单字段标签和类型（发送给 AI 服务以生成填充数据） |

### 4.4 网站内容数据的用途说明

```
扩展会读取网页表单字段的标签、名称和类型信息，将这些信息发送给用户配置的第三方 AI 服务（如 DeepSeek、OpenAI 等），以生成合适的测试填充数据。这些数据不会被扩展本身存储或用于其他用途。
```

### 4.5 数据使用目的

勾选：

```
☑️ 扩展功能相关的用途（App functionality）
```

不勾选：
```
☐ 分析
☐ 开发者通信
☐ 广告或营销
☐ 信用检测或欺诈预防
☐ 个性化
```

---

## 5. 远程代码披露

> 在 Chrome 开发者控制台 → 隐私权惯例 → 远程代码

选择：**否**

```
本扩展不使用任何远程托管的代码。所有代码都包含在扩展包内。
```

---

## 6. 完整填写示例

### 「简短说明」输入框内容：

```
AutoFormX 使用 AI 技术自动识别表单字段并生成测试数据。扩展需要访问网页以检测表单，将字段信息发送给用户配置的 AI 服务以生成填充内容。所有配置仅存储在本地浏览器。
```

---

## 审核提示

1. **如实填写**：Google 会审核权限使用是否合理
2. **保持一致**：隐私政策内容要与填写内容一致
3. **简洁明了**：说明要清晰，解释权限的必要性
4. **避免敏感词**：不要提及"收集用户数据"等可能引起误解的表述

