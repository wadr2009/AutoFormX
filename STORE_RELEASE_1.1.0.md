# AutoFormX 1.1.0 发布整理

## 本次升级内容

- 新增弹窗内模型快速切换，可在已配置 API Key 的服务商之间快速切换并记住选择。
- 新增自定义 Prompt，支持按业务场景生成更一致的整套表单测试数据。
- 支持更多 AI 服务接入方式，并提供自定义兼容接口配置。
- 优化模型列表逻辑，优先读取真实 API 返回的模型，不再展示过期的硬编码模型列表。
- 增强常见前端表单组件和原生下拉框的识别和填充。
- 优化提示词约束，避免真实敏感信息，身份证等字段仅生成测试占位数据。
- 清理模型返回内容，兼容推理模型返回的 `<think>` / `<thinking>` 内容和 Markdown 代码块。
- 修复弹窗打开时清空自定义 Prompt 的问题。
- 修复后台通义千问请求体格式。
- 收敛扩展权限，移除 `scripting` 和额外 host permissions，仅保留 `storage`、`activeTab` 和必要的 content script 页面匹配。

## 商店版本说明

建议在 Chrome Web Store 版本说明中填写：

```
1. 新增模型快速切换和自定义 Prompt，方便按业务场景生成一致的表单测试数据。
2. 支持更多 AI 服务接入方式，模型列表改为优先读取真实 API 返回结果。
3. 增强常见前端表单组件和自定义下拉框的识别和填充。
4. 优化测试数据生成提示词和推理模型返回内容清理。
5. 收敛扩展权限，移除不再需要的 scripting 与 host permissions。
6. 修复自定义 Prompt 打开弹窗后被清空、通义千问请求体格式等问题。
```

## 隐私权惯例建议

单一用途：

```
AutoFormX 是一款面向开发者和测试人员的 AI 表单测试数据生成工具。它用于识别网页表单字段，并在用户主动触发后调用用户配置的 AI 服务生成虚构测试数据并填入当前页面。
```

权限说明：

```
storage：保存用户的 API Key、服务商、模型、界面偏好和本地使用统计。
activeTab：在用户主动点击扩展、页面按钮或快捷键时，与当前活动标签页交互以执行表单填写。
content script <all_urls>：在用户访问的网页中识别表单字段并显示填写按钮，使扩展可在不同网站的测试表单中使用。
```

数据披露建议：

```
扩展会读取表单字段标签、名称、placeholder、字段约束和下拉选项，并在用户主动触发表单填写时发送给用户配置的第三方 AI 服务商，用于生成虚构测试数据。API Key、模型选择、自定义 Prompt 和界面偏好保存在浏览器扩展存储中。AutoFormX 不运营自有服务器，不出售数据，不将数据用于广告、营销或信用评估。
```
